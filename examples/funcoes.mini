// Exemplo: declaracao de funcoes (com e sem retorno) e chamadas.

int soma(int a, int b) {
    retorna a + b;
}

int dobro(int x) {
    retorna x * 2;
}

void mostrar_resultado(int valor) {
    mostrar(valor);
}

int resultado = soma(3, dobro(4));
mostrar_resultado(resultado);
